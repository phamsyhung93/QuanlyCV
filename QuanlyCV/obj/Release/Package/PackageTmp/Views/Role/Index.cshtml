@model IEnumerable<QuanlyCV.Models.Role>
<div class="main-grid">
    <div class="agile-grids">
        <!-- tables -->

        <div class="table-heading">
            <h2>Danh Sách Chức Vụ</h2>
            <h4 style="color:darkred">@TempData["error"]</h4>
        </div>
        <a  onclick="ViewCreateRole(this)"><img src="~/Content/images/add-member-icon-32.png" /></a>
        <div class="agile-tables">
            <div class="w3l-table-info">
                <table id="table">
                    <thead>
                        <tr>
                            <th class="text-center">STT</th>
                            <th class="text-center">Tên Chức Vụ</th>
                            <th class="text-center">Trạng Thái</th>
                            <th class="text-center">Thao Tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{ var stt = 1; }
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-center">@stt</td>
                                <td class="text-center">@item.RoleName</td>
                                <td class="text-center">
                                    @if (item.RoleStatus == 1)
                                    {
                                        <i class="fa fa-check" style="font-size: 20px; color: green;">Hiển Thị</i>
                                    }
                                    else
                                    {

                                        <i class="fa fa-times" style="font-size: 20px; color: red;">Không Hiển Thị</i>
                                    }
                                </td>
                                <td class="text-center">
                                    <ul style="display: flex; flex-direction: row; list-style: none; justify-content: center;">
                                        <li><a onclick="ViewUpdateRole(this)" data-id ="@item.RoleId" title="Chi tiết"><img src="~/Content/images/detail-icon-24.png" alt="Chi tiết" class="img-responsive"></a></li>
                                        <li><a onclick="DeleteRole(this)" data-id="@item.RoleId" title="Xóa"><img src="~/Content/images/delete-icon-24.png" alt="xóa"></a></li>
                                    </ul>
                                </td>
                            </tr>
                            stt++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- //tables -->
    </div>
</div>

